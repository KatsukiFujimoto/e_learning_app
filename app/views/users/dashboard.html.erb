<div class="row">
  <aside class="col-md-4 user-sidebar">
    <section class="user_info">
      <h1 id="user_show">
        <p><%= gravatar_for @user %></p>
        <p class="user-name"><%= @user.name %></p>
        <p><%= @user.email %></p>
        <%= link_to "Update your profile", edit_user_path(@user), style: "font-size: 2rem" %>
      </h1>
    </section>
    <section class="stats">
      <%= render 'shared/stats' %>
    </section>
    <section>
      <%= link_to "#{ pluralize(@category_count, "category") } learned", category_user_path(current_user), class: "categories-learned" %>
      <%= link_to "#{ pluralize(@word_count, "word") } learned", word_user_path(current_user), class: "words-learned" %>
    </section>
  </aside>
  <div class="col-md-8">
    <div class="activity-container">
      <h2 class="margin-top-zero">Activities</h2>
      <%= render partial: 'users/activity_feed', collection: @activities %>
    </div>
    <%= will_paginate @activities %>
  </div>
  </div>
</div>